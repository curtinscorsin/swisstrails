import type { HikeBuddyProfile } from "@/types";

// No public profiles are shown until real people opt in. The previous records
// were interface-demo personas and were removed so they cannot be mistaken for
// genuine Swiss Trails users.
export const HIKE_BUDDIES: HikeBuddyProfile[] = [];
